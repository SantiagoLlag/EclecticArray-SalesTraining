# Server-side keys for /api/report (session analysis). Never exposed to the browser.
# Copy to .env for local dev; set the same two variables in Vercel for production.

# ElevenLabs API key with Conversational AI read access (fetches transcripts)
ELEVENLABS_API_KEY=

# Anthropic API key (Claude grades the session)
ANTHROPIC_API_KEY=
